spring:
  application:
    name: notr
  datasource:
    url: ${JDBC_URL}
    username: ${MYSQLUSER}
    password: ${MYSQL_ROOT_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
